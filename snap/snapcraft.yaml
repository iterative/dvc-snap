name: dvc
summary: Data Version Control
description: Git for Data & Models https://dvc.org
adopt-info: dvc
confinement: classic
grade: stable
base: core20
license: Apache-2.0
layout:
  /etc/dvc:
    bind: $SNAP_DATA/etc/dvc
apps:
  dvc:
    command: bin/python3 $SNAP/bin/dvc
    completer: completion.sh
    environment:
      XDG_CONFIG_DIRS: /etc
parts:
  dvc:
    plugin: python
    source: https://github.com/iterative/dvc.git
    source-tag: 2.9.5
    build-snaps:
    - snapd
    build-packages:
    - git
    - libc-dev
    stage-packages:
    - git
    - python3
    - python3-venv
    override-pull: |
        snapcraftctl pull
        git diff --quiet || error_dirty_build
        $SNAP/usr/bin/python3 --version
        $SNAP/usr/bin/python3 $SNAPCRAFT_PROJECT_DIR/freeze.py $(pwd)/dvc
    override-build: |
        snapcraftctl build
        # TODO: hdfs oss ssh webdav
        $SNAPCRAFT_PART_INSTALL/bin/python3 -m pip install -U .[azure,gdrive,gs,ssh,s3]
        find "${SNAPCRAFT_PART_INSTALL}" -type f -executable -print0 | xargs -0 sed -i "1 s|^#\\!${SNAPCRAFT_PART_INSTALL}/bin/python3.*$|#\\!/usr/bin/env python3|"
        # make plugin aware that a file (generated later) needs to be staged
        touch $SNAPCRAFT_PART_INSTALL/completion.sh
    override-stage: |
        snapcraftctl stage
        snapcraftctl set-version $(python3 -m dvc --version)
        python3 -m dvc completion -s bash > completion.sh
